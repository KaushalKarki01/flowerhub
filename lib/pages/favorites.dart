import 'package:flutter/material.dart';

class FavotiresPage extends StatelessWidget {
  const FavotiresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favorite'),
    );
  }
}
